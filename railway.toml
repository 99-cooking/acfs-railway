[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "ttyd -p $PORT -t titleFixed='ACFS Terminal' -c ${TTYD_USER:-admin}:${TTYD_PASS:-changeme} /bin/zsh"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
